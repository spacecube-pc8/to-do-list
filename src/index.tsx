import React from 'react';
import ReactDOM from 'react-dom';
// Import App from 'App.tsx' in the same directory


ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
